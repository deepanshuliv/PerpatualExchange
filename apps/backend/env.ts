// import "dotenv/config"

// function readRequiredEnv(name: string): string {
//     const value = process.env[name];
//     if (!value) throw new Error(`Missing required env variable: ${name}`);
//     return value;
//   }

// export const port = Number(readRequiredEnv("PORT"));
// export const jwt_secret = readRequiredEnv("JWT_SECRET");
// export const reponse_stream = readRequiredEnv("RESPONSE_STREAM")
// export const request_stream = readRequiredEnv("REQUEST_STREAM")