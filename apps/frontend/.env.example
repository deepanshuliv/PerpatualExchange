# ==============================================================================
# NEXT.JS FRONTEND ENVIRONMENT VARIABLES (FOR VERCEL & LOCAL DEV)
# ==============================================================================

# Backend REST API endpoint (Proxied via Cloudflare & Nginx on GCP)
NEXT_PUBLIC_API_URL=https://exchange-api.deepanshu.live

# WebSocket endpoint for real-time orderbook, trades, and mark price streams
NEXT_PUBLIC_WS_URL=wss://exchange-api.deepanshu.live/ws
